%h2 My Channels
%ul.style3
  - @channels.each do |channel|
    %li.first
      %a{:href => "#"}= Hpricot.uxs(channel.name)
      |
      = link_to 'Edit', edit_channel_path(channel), :remote => true
      |
      = link_to 'Destroy', channel_path(channel), :confirm => 'Are you sure?', :method => :delete
