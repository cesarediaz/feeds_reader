<%= form_for(@channel, {:id => 'channel_form' }) do |f| %>
  <%= render :partial => 'shared/form_errors', :locals => {:target => @channel} %>
  <p>
    Url
  </p>
  <%= f.text_field :url %>

  <%= f.submit %>

<% end %>
